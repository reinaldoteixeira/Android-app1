<?xml version="1.0" encoding="utf-8"?>
<TableLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent" android:layout_height="fill_parent"
    android:stretchColumns="1"
    android:layout_margin="10dip">
    <TableRow>
        <TextView android:text="Alcool: " android:padding="3dip"
            android:inputType="textNoSuggestions" />
        <EditText android:id="@+id/alcool" android:padding="3dip"
            android:inputType="textNoSuggestions" />
    </TableRow>
    <TableRow>
        <TextView android:text="Gasolina: " android:padding="3dip"
            android:inputType="textNoSuggestions" />
        <EditText android:id="@+id/gasolina" android:padding="3dip"
            android:inputType="textNoSuggestions" />
    </TableRow>
    <TableRow>
        <TextView android:text="Resultado: " android:padding="3dip"
            android:inputType="textNoSuggestions" />
        <EditText android:id="@+id/resultado" android:padding="3dip"
            android:inputType="textNoSuggestions" />
    </TableRow>
    <TableRow android:gravity="right">

        <Button
            android:id="@+id/btncalcular"
            android:layout_width="wrap_content"
            android:text="Enter"
            android:onClick="btncalcular" />
    </TableRow>
</TableLayout>
